<UserControl x:Class="ePlanifv2.Views.PhotosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Views="clr-namespace:ePlanifv2.Views"
			 xmlns:ViewLib="clr-namespace:ViewLib;assembly=ViewLib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<RoutedCommand x:Key="uploadCommand"/>
	</UserControl.Resources>
	<UserControl.CommandBindings>
		<CommandBinding Command="{StaticResource uploadCommand}" CanExecute="CommandBinding_CanExecute" Executed="CommandBinding_Executed"/>
	</UserControl.CommandBindings>
	<DockPanel >
		<StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
			<Button ToolTip="Upload photo" Command="{StaticResource uploadCommand}">
				<Image Source="/ePlanifv2;component/Images/folder.png" Stretch="None"/>
			</Button>
		</StackPanel>
		<ListBox x:Name="listBox" ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedItem}" />
	</DockPanel>
</UserControl>
